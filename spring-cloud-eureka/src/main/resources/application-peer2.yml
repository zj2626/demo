server:
  port: 18091

spring:
  application:
    name: spring-cloud-eureka02
eureka:
  instance:
    hostname: peer2
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://peer1:18090/eureka/
